INSTAGRAM POST-CONNECTION TESTING GUIDE
========================================

PREREQUISITES:
- Backend running on port 5001
- Frontend running on port 5173
- Ngrok forwarding to backend
- Instagram app configured with test user

TEST FLOW:
==========

1. START FRESH
   - Clear browser cache and localStorage
   - Login to the app
   - Navigate to dashboard

2. TEST CONNECT FLOW
   - Should see "Connect Instagram" card with dashed border
   - Click "Connect Now" button
   - Should redirect to Instagram OAuth
   - Login with test user (satiksha650)
   - Click "Allow"
   - Should redirect to callback page
   - Should see "Connecting Instagram" spinner
   - Should see "Success!" message
   - Should redirect to dashboard after 2 seconds

3. VERIFY DASHBOARD
   - Should see green toast: "Instagram connected successfully! 🎉"
   - Toast should auto-dismiss after 5 seconds
   - Should see Instagram connected card with:
     * Instagram gradient logo (purple-pink-orange)
     * "Connected" green badge
     * @username displayed
     * Posts count
     * Followers count (may be 0)
     * Account type (Personal/Business/Creator)
     * Connected date
     * "View Analytics" button
     * "Disconnect" button

4. TEST VIEW ANALYTICS
   - Click "View Analytics" button
   - Should navigate to /analytics page

5. TEST DISCONNECT
   - Go back to dashboard
   - Click "Disconnect" button
   - Should see confirmation dialog
   - Click "OK"
   - Should see toast: "Instagram disconnected successfully"
   - Instagram card should change to "Connect Instagram" prompt

6. TEST RECONNECT
   - Click "Connect Now" again
   - Should go through OAuth flow
   - Should reconnect successfully
   - Should see connected card again

7. TEST ERROR HANDLING
   - Check browser console for errors
   - Check backend logs for errors
   - Verify no 500 errors on:
     * GET /api/profile
     * GET /api/content/stats
     * GET /api/platforms/instagram/profile

8. TEST REFRESH
   - Refresh the dashboard page
   - Instagram card should persist
   - Profile data should reload

EXPECTED BACKEND LOGS:
======================
✓ State validated successfully
✓ Short-lived token received
✓ Long-lived token received
✓ Profile received: @username
✓ Instagram connection saved successfully
=== TOKEN EXCHANGE COMPLETE ===

EXPECTED FRONTEND BEHAVIOR:
===========================
- No console errors
- Smooth transitions
- Toast appears and dismisses
- Instagram card displays correctly
- All buttons work
- Loading states show appropriately

COMMON ISSUES:
==============
1. "No Instagram account connected" error
   - Check if token exchange completed
   - Check backend logs for errors
   - Verify database has InstagramConnection record

2. Profile data not showing
   - Check if API call succeeds
   - Check browser network tab
   - Verify JWT token is valid

3. Toast not appearing
   - Check if URL has ?instagram=connected parameter
   - Check if useEffect is running
   - Check browser console for errors

4. 500 errors on profile/stats
   - Check backend logs for stack traces
   - Verify datetime timezone handling
   - Check database connection

SUCCESS CRITERIA:
=================
✓ OAuth flow completes without errors
✓ Success toast appears on dashboard
✓ Instagram card shows profile data
✓ All stats display correctly
✓ Buttons work (View Analytics, Disconnect, Connect)
✓ No 500 errors in backend
✓ No console errors in frontend
✓ Smooth user experience throughout
