DATETIME TIMEZONE FIX - QUICK SUMMARY
=====================================

PROBLEM: "can't compare offset-naive and offset-aware datetimes"

SOLUTION: Multi-layer defensive timezone handling

FILES CHANGED:
1. backend/models.py - Added TZDateTime custom type
2. backend/platforms/instagram/instagram_model.py - Enhanced is_valid() and __init__()
3. backend/platforms/instagram/instagram_controller.py - Added defensive timezone checks
4. backend/services/mongodb_service.py - Fixed 5 datetime.utcnow() calls
5. backend/routes/linkogenei.py - Fixed 1 datetime.utcnow() call

KEY CHANGES:
- All datetime.utcnow() → datetime.now(timezone.utc)
- Added custom TZDateTime type for SQLAlchemy
- Added defensive timezone checks in OAuthState.is_valid()
- Added defensive timezone checks in Instagram controller
- All datetime comparisons now use timezone-aware datetimes

TO TEST:
1. cd backend
2. python app.py
3. Test Instagram OAuth flow
4. Should see "✓ State validated successfully" in logs
5. Should NOT see datetime comparison errors

WHAT TO WATCH FOR:
- If you see "Fixed timezone-naive expires_at" warning, it's fixing old data (normal)
- New states will be created with proper timezone info
- Token exchange should complete successfully

STATUS: ✅ COMPLETE - Ready to test
